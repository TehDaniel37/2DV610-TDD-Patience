The game is played with a deck containing 52 unique cards.

A stack can only be merged with another stack if the top cards of the stacks have equal color OR value and the stacks have to be either next to eachother OR have two stacks in between.

The goal of the game is to only have one stack when there is no cards left in the deck, if there are more than one stack the player lose.
